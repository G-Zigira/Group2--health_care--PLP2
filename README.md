Quick run:

Make sure you have Python 3 installed.

Save the files from this document into a folder (keep filenames exactly as shown).

In a terminal, run:

python main.py

The app uses SQLite (no extra installs). On first run it will create clinico.db and seed a few hospitals.